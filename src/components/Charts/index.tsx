import TagCloud from './TagCloud';
import WaterWave from './WaterWave';
import Bar from './Bar';
import SalesCard from './SalesCard';

const Charts = {
  TagCloud,
  WaterWave,
  Bar,
  SalesCard,
};

export { Charts as default, TagCloud, WaterWave, Bar, SalesCard };
